<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mki.kehrwochenprojekt.mobilecomputing_sose17">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ManageTaskActivity" />
        <activity android:name=".ManageGroupActivity" />
        <activity android:name=".CreateTaskActivity" />
        <activity android:name=".ManageMyTasks" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AddGroupActivity" />
        <activity android:name=".ConfirmTaskActivity" />
        <activity android:name=".SelectTaskToConfirmActivity" />
        <activity android:name=".ManageSingleResident" />
        <activity android:name=".SelectResidentToManage"></activity>
    </application>

</manifest>